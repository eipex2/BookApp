ngDescribe({
    name: 'Test toolbar component',
    modules: 'app',
    element: '<toolbar></toolbar>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
