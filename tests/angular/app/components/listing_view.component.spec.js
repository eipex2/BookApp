ngDescribe({
    name: 'Test listing-view component',
    modules: 'app',
    element: '<listing-view></listing-view>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
