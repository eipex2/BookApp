ngDescribe({
    name: 'Test message-box component',
    modules: 'app',
    element: '<message-box></message-box>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
