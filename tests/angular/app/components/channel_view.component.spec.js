ngDescribe({
    name: 'Test channel-view component',
    modules: 'app',
    element: '<channel-view></channel-view>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
