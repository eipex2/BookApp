ngDescribe({
    name: 'Test ChannelManagerService',
    modules: 'app',
    inject: 'ChannelManagerService',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
