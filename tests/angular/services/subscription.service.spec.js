ngDescribe({
    name: 'Test SubscriptionService',
    modules: 'app',
    inject: 'SubscriptionService',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
